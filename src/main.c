#include <unistd.h>
#include <stdio.h>

int main(int argc, char **argv)
{
    //sayı degeri al
    //girilen sayı adedi değişiklik gösterebilir.
    //her sayi farklı kolonideki popülasyonu gösterir.
    //her koloni rastgele bir karakter ile gösterilmeli
    //her koloni farklı bir savaş taktiği kullanabilir.
    //(Bu taktik yapısından (struct)
    //üretilmiş bir değişken değil, taktik yapısından kalıtım alan (benzetim) farklı bir türdür.).
}   